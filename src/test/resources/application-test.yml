app:
  dynamodb:
    table-name: franchises-test
    region: us-east-1
    create-table-on-startup: false
    endpoint: http://localhost:8000

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    root: WARN
    co.com.nequi.franchise: DEBUG
